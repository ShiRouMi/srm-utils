import {
    getStorage,
    setStorage,
    removeStorage,
    getSession,
    setSession,
    removeSession
} from './storage';

const F = {
    getStorage, setStorage, removeStorage,
    getSession, setSession, removeSession
}

export default F;